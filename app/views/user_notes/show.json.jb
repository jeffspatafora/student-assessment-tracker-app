{
  id: @user_note.id,
  user: @user_note.user.name,
  student: @user_note.student.name,
  student_email: @user_note.student.email,
  project: @user_note.project.title,
  note: @user_note.note,
  created_at: @user_note.created_at,
  updated_at: @user_note.updated_at
}
